import Zigly1 from "../components/zigly/Zigly1"

export default {
    title: 'Zigly/Design 1',
    component: Zigly1,
}

const Template = (args) => <Zigly1 {...args} />

export const Primary = Template.bind({})